import { render, screen, cleanup, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom';

test(handleOpenWidget,() => {
    it("widget opens when upload button is clicked", () => {
    })
});